<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- pageTitle %> - تارگو</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #0f0f11;
            --secondary: #1a1a1d;
            --accent: #2563eb;
            --accent-light: #3b82f6;
            --text: #ffffff;
            --text-muted: #a1a1aa;
            --border: #27272a;
            --glass: rgba(16, 16, 18, 0.8);
        }

        * {
            scrollbar-width: thin;
            scrollbar-color: var(--border) transparent;
        }

        *::-webkit-scrollbar {
            width: 4px;
        }

        *::-webkit-scrollbar-track {
            background: transparent;
        }

        *::-webkit-scrollbar-thumb {
            background: var(--border);
            border-radius: 2px;
        }

        body {
            background: linear-gradient(135deg, var(--primary) 0%, #0a0a0c 100%);
            color: var(--text);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            font-weight: 400;
            line-height: 1.6;
        }

        .glass {
            background: var(--glass);
            backdrop-filter: blur(16px);
            border: 1px solid var(--border);
        }

        .nav-blur {
            background: rgba(15, 15, 17, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.2s ease;
            text-decoration: none;
        }

        .btn-primary {
            background: var(--accent);
            color: white;
            padding: 0.625rem 1.25rem;
        }

        .btn-primary:hover {
            background: var(--accent-light);
            transform: translateY(-1px);
        }

        .nav-link {
            padding: 0.5rem 1rem;
            border-radius: 6px;
            transition: all 0.2s ease;
            color: var(--text-muted);
            font-weight: 500;
        }

        .nav-link:hover {
            color: var(--text);
            background: rgba(255, 255, 255, 0.05);
        }

        .search-box {
            background: var(--secondary);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 0.625rem 1rem;
            color: var(--text);
            font-size: 0.875rem;
            transition: all 0.2s ease;
            width: 240px;
        }

        .search-box:focus {
            outline: none;
            border-color: var(--accent);
            background: rgba(37, 99, 235, 0.05);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--accent), var(--accent-light));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .mobile-menu {
            background: var(--secondary);
            border: 1px solid var(--border);
            border-radius: 12px;
        }
    </style>
</head>
<body class="antialiased">

<div class="main-container min-h-screen flex flex-col">

<header class="sticky top-0 z-50 nav-blur">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex items-center justify-between h-16">

            <!-- Logo -->
            <a href="/" class="logo hover:scale-105 transition-transform">
                تارگو
            </a>

            <!-- Desktop Navigation -->
            <nav class="hidden md:flex items-center space-x-6 space-x-reverse">
                <a href="/" class="nav-link">خانه</a>
                <a href="/mangas" class="nav-link">مانگاها</a>
                <a href="/channels" class="nav-link">کانال‌ها</a>
            </nav>

            <!-- Search & Actions -->
            <div class="hidden md:flex items-center space-x-4 space-x-reverse">
                <form action="/mangas" method="GET" class="flex items-center space-x-2 space-x-reverse">
                    <input type="search" name="search" placeholder="جستجو کنید..." 
                           class="search-box" 
                           value="<%= typeof query !== 'undefined' ? query : '' %>">
                    <button type="submit" class="btn btn-primary px-3">
                        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </button>
                </form>

                <% if (user) { %>
                    <div class="flex items-center space-x-3 space-x-reverse">
                        <a href="/account" class="flex items-center space-x-2 space-x-reverse nav-link">
                            <% if (user.profile_image_url) { %>
                                <img src="<%= user.profile_image_url %>" alt="پروفایل" class="w-8 h-8 rounded-full">
                            <% } else { %>
                                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-blue-600 flex items-center justify-center">
                                    <span class="text-xs font-bold"><%= user.app_username.charAt(0).toUpperCase() %></span>
                                </div>
                            <% } %>
                            <span class="text-sm"><%= user.app_username %></span>
                        </a>
                        <a href="/logout" class="text-red-400 hover:text-red-300 p-2 rounded-md transition-colors">
                            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7" />
                            </svg>
                        </a>
                    </div>
                <% } else { %>
                    <a href="/login" class="btn btn-primary">ورود</a>
                <% } %>
            </div>

            <!-- Mobile Menu Button -->
            <button type="button" id="mobile-menu-btn" class="md:hidden p-2 rounded-md text-gray-400 hover:text-white transition-colors">
                <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div class="md:hidden hidden mobile-menu mx-4 mb-4 p-4" id="mobile-menu">
        <form action="/mangas" method="GET" class="mb-4">
            <div class="flex space-x-2 space-x-reverse">
                <input type="search" name="search" placeholder="جستجو کنید..." 
                       class="search-box flex-1" 
                       value="<%= typeof query !== 'undefined' ? query : '' %>">
                <button type="submit" class="btn btn-primary px-3">
                    <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                    </svg>
                </button>
            </div>
        </form>

        <nav class="space-y-1">
            <a href="/" class="nav-link block">خانه</a>
            <a href="/mangas" class="nav-link block">مانگاها</a>
            <a href="/channels" class="nav-link block">کانال‌ها</a>
            <% if (user) { %>
                <a href="/account" class="nav-link block">حساب کاربری</a>
                <a href="/logout" class="nav-link block text-red-400">خروج</a>
            <% } else { %>
                <a href="/login" class="nav-link block">ورود</a>
            <% } %>
        </nav>
    </div>
</header>

<main class="flex-1">

<script>
document.getElementById('mobile-menu-btn')?.addEventListener('click', () => {
    document.getElementById('mobile-menu')?.classList.toggle('hidden');
});
</script>

</main>
</div>
</body>
</html>