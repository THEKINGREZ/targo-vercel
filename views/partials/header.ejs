<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- pageTitle %> - کتابخانه مانگا</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playpen+Sans+Arabic&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #18181b; /* zinc-900 */
            color: #e4e4e7; /* zinc-200 */
            font-family: 'Playpen Sans Arabic', sans-serif;
        }
        .header-rounded-box {
            background-color: #27272a; /* zinc-800 */
            border-radius: 1.5rem;
            padding: 1rem 2rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .playpen-sans-arabic {
            font-family: 'Playpen Sans Arabic', sans-serif;
        }
        #mobile-menu {
            background-color: #27272a;
            border-radius: 1rem;
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.2);
            margin-top: 0.75rem;
            margin-left: auto;
            margin-right: auto;
            max-width: 90%;
            padding: 1rem;
        }
        #mobile-menu a {
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 0.5rem;
            padding: 0.625rem 1rem;
            margin-bottom: 0.5rem;
            width: 100%;
            transition: background-color 200ms ease-in-out, color 200ms ease-in-out;
        }
    </style>
</head>
<body class="antialiased">

<div class="main-container min-h-screen flex flex-col">

<header class="sticky top-0 z-50">
    <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-center h-24">
            <div class="header-rounded-box flex items-center justify-between w-full md:max-w-7xl mx-auto bg-zinc-800/80 backdrop-blur-md">

                <!-- Right Section: Nav links (Desktop) / Hamburger (Mobile) -->
                <div class="flex-1 flex justify-start">
                    <!-- Desktop Nav -->
                    <div class="hidden md:flex items-baseline space-x-4 space-x-reverse">
                        <a href="/" class="text-white bg-zinc-700 hover:bg-sky-600 px-4 py-2.5 rounded-lg text-sm font-medium inline-flex items-center justify-center transition-colors duration-200">صفحه اصلی</a>
                        <a href="/mangas" class="text-white bg-zinc-700 hover:bg-sky-600 px-4 py-2.5 rounded-lg text-sm font-medium inline-flex items-center justify-center transition-colors duration-200">همه مانگاها</a>
                        <a href="/channels" class="text-white bg-zinc-700 hover:bg-sky-600 px-4 py-2.5 rounded-lg text-sm font-medium inline-flex items-center justify-center transition-colors duration-200">کانال‌ها</a>
                    </div>
                    <!-- Mobile Hamburger Button -->
                    <div class="flex md:hidden">
                        <button type="button" id="mobile-menu-button" class="bg-zinc-800 inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-zinc-800 focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
                            <span class="sr-only">باز کردن منوی اصلی</span>
                            <svg class="block h-6 w-6" id="menu-open-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" /></svg>
                            <svg class="hidden h-6 w-6" id="menu-close-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" /></svg>
                        </button>
                    </div>
                </div>

                <!-- Center Section: Site Name -->
                <div class="flex-shrink-0 mx-4">
                    <a href="/" class="flex-shrink-0 flex items-center space-x-2 space-x-reverse">
                        <span class="text-white font-bold text-3xl hover:text-sky-300 transition-colors playpen-sans-arabic">
                            تارگو
                        </span>
                    </a>
                </div>

                <!-- Left Section: Search and User actions -->
                <div class="flex-1 flex justify-end relative z-10">
                    <!-- Desktop Search & User Actions -->
                    <div class="hidden md:flex items-center space-x-4 space-x-reverse">
                        <form action="/mangas" method="GET" class="flex items-center space-x-2 space-x-reverse">
                            <input type="search" name="search" placeholder="جستجوی مانگا..." class="p-2 rounded-md bg-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-transparent transition-all duration-200" value="<%= typeof query !== 'undefined' ? query : '' %>">
                            <button type="submit" class="bg-sky-500 text-white p-2 rounded-md hover:bg-sky-600 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:ring-offset-2 focus:ring-offset-zinc-800 transition-colors duration-200">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>
                            </button>
                        </form>

                        <div class="flex items-center space-x-4 space-x-reverse ml-4">
                            <% if (user) { %>
                                <a href="/account" class="flex items-center gap-2 text-sm font-medium text-zinc-200 hover:text-white transition-colors">
                                    <% if (user.profile_image_url) { %>
                                        <img src="<%= user.profile_image_url %>" alt="آواتار" class="w-8 h-8 rounded-full object-cover border-2 border-zinc-600">
                                    <% } else { %>
                                        <div class="w-8 h-8 rounded-full bg-zinc-700 flex items-center justify-center">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" /></svg>
                                        </div>
                                    <% } %>
                                    <span><%= user.app_username %></span>
                                </a>
                                <a href="/logout" class="text-white bg-red-600 hover:bg-red-700 px-4 py-2.5 rounded-lg text-sm font-medium inline-flex items-center justify-center transition-colors duration-200">خروج</a>
                            <% } else { %>
                                <a href="/login" class="bg-sky-500 text-white hover:bg-sky-600 px-4 py-2.5 rounded-lg text-sm font-medium inline-flex items-center justify-center transition-colors duration-200">ورود</a>
                            <% } %>
                        </div>
                    </div>
                    <!-- Mobile User Actions -->
                    <div class="flex items-center md:hidden">
                        <% if (user) { %>
                            <a href="/account" class="w-10 h-10 rounded-full flex items-center justify-center bg-zinc-700 overflow-hidden focus:outline-none focus:ring-2 focus:ring-sky-500 focus:ring-offset-2 focus:ring-offset-zinc-800">
                                <% if (user.profile_image_url) { %>
                                    <img src="<%= user.profile_image_url %>" alt="آواتار" class="w-full h-full object-cover">
                                <% } else { %>
                                    <span class="sr-only">حساب کاربری</span>
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                    </svg>
                                <% } %>
                            </a>
                        <% } else { %>
                            <a href="/login" class="bg-sky-500 text-white hover:bg-sky-600 px-4 py-2.5 rounded-lg text-sm font-medium inline-flex items-center justify-center transition-colors duration-200">ورود</a>
                        <% } %>
                    </div>
                </div>

            </div>
        </div>
    </nav>

    <!-- Mobile Menu, shown on toggle -->
    <div class="md:hidden hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
            <form action="/mangas" method="GET" class="flex items-center space-x-2 space-x-reverse w-full mb-3">
                <input type="search" name="search" placeholder="جستجوی مانگا..." class="flex-1 min-w-0 p-2.5 rounded-lg bg-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-transparent transition-all duration-200" value="<%= typeof query !== 'undefined' ? query : '' %>">
                <button type="submit" class="flex-shrink-0 bg-sky-500 text-white p-2 rounded-lg hover:bg-sky-600 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:ring-offset-2 focus:ring-offset-zinc-800 transition-colors duration-200">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>
                </button>
            </form>
            <a href="/" class="text-white bg-zinc-700 hover:bg-sky-600 block font-medium">صفحه اصلی</a>
            <a href="/mangas" class="text-white bg-zinc-700 hover:bg-sky-600 block font-medium">همه مانگاها</a>
            <a href="/channels" class="text-white bg-zinc-700 hover:bg-sky-600 block font-medium">کانال‌ها</a>
        </div>
    </div>
</header>

<main class="flex-grow">
    
<!-- The rest of your page content goes here -->

<script>
document.addEventListener('DOMContentLoaded', function () {
    const menuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    const openIcon = document.getElementById('menu-open-icon');
    const closeIcon = document.getElementById('menu-close-icon');

    if (menuButton && mobileMenu && openIcon && closeIcon) {
        menuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
            openIcon.classList.toggle('hidden');
            openIcon.classList.toggle('block');
            closeIcon.classList.toggle('hidden');
            closeIcon.classList.toggle('block');
            const isExpanded = menuButton.getAttribute('aria-expanded') === 'true';
            menuButton.setAttribute('aria-expanded', !isExpanded);
        });
    }
});
</script>
